# 実装と検証の状態

## 現在の構成と確認済みの範囲

トップを背景動画専用に変更した。見出しと「制作実績」を映像内に置き、現在位置と3本の進捗バー、一時停止・再開だけを表示する。横映像は背景を満たす `cover`、縦映像は全体を残す `contain`。全編プレーヤー、動画一覧、動画カードへの導線は撤去した。「Y」の四角いロゴを削除した状態も保持している。

- 静的HTML28ページ、検索対象27ページ、ローカル参照1636件。最新のオフライン監査はエラー・警告とも0。
- 背景専用カタログ3件の実ファイル・項目検証に成功。必須項目は `title/src/poster/width/height`、任意は `slug`。4種類の正常入力と、旧全編用項目を含む15種類の不正入力の一時テストにも成功した。
- `videos.html`、`videos/hitoiki-soda.html`、`videos/haircare-ad.html`、`videos/pov-video.html` と `scripts/build_videos.py` を削除。対応する拡張子なしURLのリダイレクト、フッター、サービス本文、実績ページ内の動画再生リンクも撤去した。
- 新しい背景用ファイルは、以下の実メタデータと音声ストリームがないことを `ffprobe` で確認した。
- 新しい背景専用レイアウトを27ページ・57表示条件でブラウザ検証。横はみ出し、画像欠損、H1重複、旧動画リンク、Yマーク、ブラウザエラーは0。メニュー、FAQ、メール下書き、ガイドコピー、目次も成功。
- 3本の実動画で01→02→03→01の自動循環、8.6秒間の手動停止維持と再開、画面外停止と復帰を確認。動画は常時ミュート・インライン、ネイティブ再生操作なし。動きを減らす設定では手動再開まで動画の通信0、JavaScript無効でも見出しとポスターを表示して動画の通信0。
- 1482／1024／768／390／320pxで、見出しと操作の衝突や動画切り替えによるレイアウト移動がない。旧4視聴URLの404も確認。1482×1061と390×1000の最終画面を保存し、採用画像と比較した。
- 追加の二本線メニューを1482／390／320pxで検証。トップの映像は画面上端から始まり、文字ロゴは映像内、右端の白帯なし。メニューから背後のページへTabフォーカスが移らず、Escape・閉じるボタン・外側クリックで元のボタンへ戻る。同じページのサービスリンクは閉じてから対象へスクロール・フォーカスし、別ページのリンクも正常。JavaScriptなしの常設リンク、全27検索対象ページから旧メニュー構造がなくなったことも確認。ブラウザエラー0。
- メニューの追加調整も検証済み。二本線は通常・ホバーとも背景やぼかしなし、左右5pxのずれを確認。開き始め150ms、パネル表示700ms、項目まで完了1200ms、閉じ始め100msと完了400msを測定し、閉じる途中もモーダル状態を保持する。Escape・連続開閉・サービスページ遷移・同ページのスクロールとフォーカスに成功。320×640でサービス8件・サイト案内4件・無料相談の全13リンクへ到達でき、動きを減らす設定とJavaScriptなしでも操作可能。今回のブラウザエラー0。IABと別ブラウザのPC・スマートフォン画像でも見え方を確認した。

## 現在のローカル確認用の背景動画

ユーザーが指定した完成動画から、背景用の短い音声なしMP4を別に作成した。3本ともH.264。元動画はDownloads内で変更せず保管し、以前の全編配信用ファイルはWebルートから外してレビュー用ディレクトリに保管している。

| 作品 | 背景用ファイル | サイズ | 解像度／フレーム数 | 再生時間 | 音声ストリーム |
| --- | --- | --- | --- | --- | --- |
| ひと息ソーダ | `hitoiki-soda-background.mp4` | 1,346,516 bytes | 1280×720／194 | 8.083333秒 | なし |
| ヘアケア商品の縦型AI広告 | `ai-sample-background.mp4` | 6,233,291 bytes | 1072×1920／482 | 8.033333秒 | なし |
| 日常を切り取る実写風POV動画 | `workplace-pov-background.mp4` | 2,114,325 bytes | 720×1280／194 | 8.083333秒 | なし |

この表は現在のファイルを測った結果である。ブラウザ再生・画面表示の確認は上記の別検証で実施した。背景用動画は先頭約8秒の映像ストリームを再圧縮せずコピーし、音声を除去して全尺デコードに成功した。下記の全編ファイルに対するSSIM結果とは区別する。

## 旧構成で完了していたブラウザ検証

以下は、動画一覧と全編視聴ページを備えていた前の構成の履歴。現在の仕様から撤去した機能も含む。

- 静的HTML32ページ、検索対象31ページ、ローカル参照1353件のオフライン監査がエラー・警告なし。
- PC／スマートフォンの全31ページと補助サイズの合計64表示で、横はみ出し・画像切れ・取得失敗なし。
- メニューとEscape、FAQ、メール下書き（送信なし）、ガイドのコピー、目次をブラウザで確認。
- 3本の無音ヒーロー再生、01→02→03への切替、前後操作、一時停止維持、全編プレーヤーでの実再生を確認。ブラウザエラーなし。
- 1505／1024／768／390／320pxで映像枠と下の要素の寸法が安定。低減モーション時の初期動画取得なし、手動再生、JavaScriptなしのポスターと全編リンクも確認済み。

## 旧全編配信用ファイルの比較記録

以前の全編MP4では、ユーザー提供の完成動画に対し、解像度・全フレーム数・各フレーム時刻・AAC音声パケットと音声時刻の一致、全尺デコードを確認した。以下のSSIMは、現在の短い背景ファイルではなく、撤去した全編配信用ファイルの再圧縮比較である。

| 作品 | 旧全編サイズ | 解像度／フレーム数 | 最低／平均フレームSSIM |
| --- | --- | --- | --- |
| ひと息ソーダ | 3,075,014 bytes | 1280×720／481 | 0.984593／0.989767 |
| ヘアケア商品の縦型AI広告 | 28,294,379 bytes | 1072×1920／2518 | 0.981956／0.989462 |
| 日常を切り取る実写風POV動画 | 5,342,544 bytes | 720×1280／481 | 0.984702／0.987826 |

旧全編の再生時間は20.063991秒／41.966667秒／20.016667秒。3本目の元ファイル名には16:9・約15秒とあるが、実ファイルは720×1280・約20秒だった。公開日は推測していない。現在は背景用途のためVideoObjectを生成せず、カタログにも全編の公開日や尺を登録しない。

## 公開と検索

ユーザーのVercelへの反映依頼を受け、完成版コミット `eb96c93` を既存プロジェクト `yuki-portfolio` の本番に公開した。デプロイ `dpl_5aXoLnbaUz8c8Jj3AWzGFr7zFSDP` はReady、`https://taguchi338.com` へ割り当て済み。GitHubの既存作業ブランチ `codex/ai-search-refresh` にも同じコードをpushした。本番はVercel CLIからの公開で、GitHubのmainは変更していない。

本番配信45項目を確認。27検索対象ページとCSS・JavaScript・robots.txt・サイトマップがローカルの完成版と一致し、200応答。撤去した4視聴URLと内部資料の代表3URLは404。旧形式3URLのリダイレクト、背景動画3件のRangeリクエストによる206応答も正常。本番ブラウザでも新メニューの表示と自動動画編集ページへの移動を確認した。検証結果はWebルート外の `taguchi338-review/production-deployment-check.json` に保存。

Google検索の主要6語句の最初の結果ページと、Search Consoleで検索流入・インデックス・生成AI機能の過去の状況を確認した。今回の変更による効果とは区別する。実ユーザーのCore Web Vitalsには評価できるデータがなく、良好とは確認していない。非公開の検索・流入データはWebルート外のレビュー資料に保存し、公開リポジトリへ含めない。技術実装の完了を競合優位や掲載保証とは扱わない。運用項目は `search-strategy.md`、背景動画の追加手順は `video-addition.md` を参照。

## 実績の追加と表現の見直し

本人が述べた経営者向けAIエージェント初期設定・セットアップ・実務活用支援とAI漫画制作を、実績一覧の短い紹介として追加した。トップ・プロフィール・AIエージェントサービスから関連づけた。新しい実例の詳細な工程、比較画面、効果・工数は推測で追加していない。

既存の実績一覧と11件の詳細から、この作業で根拠を確認できない削減率・速度・成果の断定を外し、担当内容を中心に修正した。同じ表現が残っていたAIインフルエンサーサービスも更新した。数値入りの旧バナー2種類は公開HTML・生成元からの参照を外した。過去の全案件の実施内容を原資料まで照合した監査ではない。

追加部分はPC表示と390／320pxの3ページ・6表示条件を確認し、横はみ出し・文字切れなし。トップの2リンクから実績内の見出し、実績からサービスと問い合わせの計4遷移に成功。要約生成を再実行しても重複しないこと、旧バナー参照が公開HTMLと生成元に残っていないことを確認した。オフライン監査とサイトマップの27canonical照合も成功した。

この追加版コミット`e8c697d`を本番へ公開した。デプロイ`dpl_ECVjUVxM1x8peauqEEngz8i9eyKy`はReady、`https://taguchi338.com`へ割り当て済み。全27ページとCSS・JavaScript・robots・サイトマップがローカルと一致。旧形式4URLの308転送、旧動画ページ・内部資料・旧バナーの計11URLの404を含む46項目に成功した。旧バナーの元ファイルは保存し、Vercelの公開対象から外している。本番ブラウザでも実績2件の表示を確認した。

Search Consoleで同じサイトマップURLを再送信し、「サイトマップを送信しました」を確認。AIエージェントサービスURLのインデックス登録リクエストも受け付けられ、優先クロールキューへの追加が表示された。これは実際のインデックス登録や検索掲載の完了を意味しない。再送信前の監査と送信受付の記録は、Webルート外のレビュー資料に分けて保存した。
